apply plugin: 'findbugs'

findbugs {
    toolVersion = '3.0.0'
    ignoreFailures = true
    effort = 'max'
}

tasks.withType(FindBugs) {
    reports {
        xml.enabled = false
        html.enabled = true
    }
}

